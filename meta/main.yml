---
galaxy_info:
  role_name: "nginx"
  author: "cornfeedhobo"
  description: "Install and configure nginx"
  license: "MIT"
  galaxy_tags: ["nginx", "webserver"]
  min_ansible_version: "2.5"
  platforms:
    -
      name: "EL"
      versions:
        - "all"
    -
      name: "Fedora"
      versions:
        - "all"
    -
      name: "Ubuntu"
      versions:
        - "all"

dependencies:
  - role: "jtyr.config_encoder_filters"
    tags: ["nginx", "nginx-configure"]
